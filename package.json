{
  "name": "codex-app",
  "version": "0.1.0",
  "description": "Cross-platform Codex desktop client",
  "author": "",
  "license": "Apache-2.0",
  "type": "commonjs",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "concurrently -k \"npm:dev:main\" \"npm:dev:preload\" \"npm:dev:renderer\" \"npm:dev:electron\"",
    "dev:main": "tsup src/main/main.ts --tsconfig tsconfig.node.json --format cjs --platform=node --target=node18 --sourcemap --watch --out-dir dist/main --env.NODE_ENV=development --external electron",
    "dev:preload": "tsup src/preload/index.ts --tsconfig tsconfig.node.json --format cjs --platform=node --target=node18 --sourcemap --watch --out-dir dist/preload --env.NODE_ENV=development --external electron",
    "dev:renderer": "vite --config vite.config.ts",
    "dev:electron": "wait-on dist/main/main.js dist/preload/index.js http://localhost:5173 && electron .",
    "build": "rimraf dist && npm run build:main && npm run build:preload && npm run build:renderer",
    "build:main": "tsup src/main/main.ts --tsconfig tsconfig.node.json --format cjs --platform=node --target=node18 --sourcemap --out-dir dist/main --env.NODE_ENV=production --external electron",
    "build:preload": "tsup src/preload/index.ts --tsconfig tsconfig.node.json --format cjs --platform=node --target=node18 --sourcemap --out-dir dist/preload --env.NODE_ENV=production --external electron",
    "build:renderer": "vite build",
    "package": "npm run build && electron-builder",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.44.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^8.2.2",
    "electron": "^30.0.0",
    "electron-builder": "^24.13.3",
    "rimraf": "^5.0.10",
    "tsup": "^8.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "wait-on": "^9.0.1"
  },
  "build": {
    "appId": "com.example.codexapp",
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.developer-tools"
    },
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Development"
    }
  }
}